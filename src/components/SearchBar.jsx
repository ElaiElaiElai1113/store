import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input type="text" />
      <div>
        <input type="checkbox" />
        Only shows products in stocks
      </div>
    </div>
  );
};

export default SearchBar;
